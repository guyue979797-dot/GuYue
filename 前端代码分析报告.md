# GuYue / InfoLens 前端代码全面分析报告

> 分析时间：2026-08-02 ｜ 分析对象：`guyue979797-dot/GuYue` 仓库完整代码
> 主要分支：`codex/add-wecom-distribution-workflow`（含完整 React 前端源码）

---

## 一、分析范围与总体结论

### 1.1 仓库与分支情况

仓库共有两个远程分支：

| 分支 | 状态 | 前端形态 |
|---|---|---|
| `main` | 落后 18 个提交 | 旧版单文件应用（`InfoLens/web/index.html`，903 行纯 HTML+JS） |
| `codex/add-wecom-distribution-workflow` | 最新完整代码 | React 19 SPA + Arco Design（`InfoLens/frontend/`） |

**重要提示：** `main` 分支与 `codex` 分支的前端形态完全不同，且 `main` 已严重落后。当前部署/开发应统一到 `codex/add-wecom-distribution-workflow`，否则会出现"改了 main 看不到效果"或"部署源混淆"的问题。建议将 `main` 合并或归档，保持单一事实源。

### 1.2 前端文件规模

| 文件 | 行数 | 说明 |
|---|---:|---|
| `frontend/src/App.jsx` | 5,427 | 全部业务页面 + 组件 + 工具函数（巨石文件） |
| `frontend/src/styles/legacy.css` | 2,537 | 旧样式，与新版样式大量重叠 |
| `frontend/src/styles/snow-outbound.css` | 980 | 雪花出库样式 |
| `frontend/src/styles/customers.css` | 854 | 客户/冻结列样式 |
| `frontend/src/styles/products.css` | 560 | 产品样式 |
| `frontend/src/styles/common.css` | 229 | 通用样式 |
| `frontend/src/pages/Users.jsx` | 211 | 权限管理页 |
| `frontend/src/components/TableActions.jsx` | 43 | 行操作组件 |
| `frontend/src/styles/*.css`（其余） | ~220 | tokens/base/layout/users |
| 构建产物 `web/assets/` | ~6.4MB | 含 3 个历史遗留 JS 包 |

前端源码合计约 **11,000 行**（JSX + CSS），全部由功能迭代累积而成。

### 1.3 技术栈

- React `19.2.7` + react-dom `19.2.7`
- Arco Design `@arco-design/web-react 2.66.15`
- 构建：自定义 `node build.mjs`（esbuild 直打包），Vite 7 仅用于本地开发
- 部署：Flask 静态托管 `web/index.html` + `web/assets/`，登录页服务端渲染
- 无 TypeScript、无 ESLint/Prettier、无前端测试、无 CI

### 1.4 总体评价

**优点突出：**

- 安全基线做得不错：CSRF 双重校验（Header + Body）、会话 Cookie `HttpOnly + SameSite=Lax + production Secure`、CSP/X-Frame-Options/nosniff 等安全头齐全、React 默认转义（全库无 `dangerouslySetInnerHTML`）。
- 部分工程实践优秀：图片懒加载 + 相邻页 `requestIdleCallback` 预取、请求竞态序号防护、前端缓存 Map（上限 24 条）、批量任务 localStorage 断点恢复、手机号脱敏展示、下载文件名从 `Content-Disposition` 解析并回收 blob URL。
- 导入/导出"解析预览 → 确认提交"两步式交互设计合理，降低误操作风险。

**主要问题（详见下文）：**

- `App.jsx` 5,427 行巨石组件，页面/组件/工具函数混在一个文件，且通过 `window.React`/`window.arco` 全局变量取依赖。
- 年份硬编码为 2026（3 处），2027 年政策功能将不可用（时间炸弹）。
- 批量提取轮询无超时、无取消、无卸载保护。
- 存在约 400 行永不可达的死代码（雪花模板规则 UI 被 `{false ? ...}` 禁用）。
- 业务员名单前端硬编码，与后端接口下发数据双份维护，必然漂移。
- 构建产物（约 1MB 的 arco.min.js + 两个共 1MB 的孤儿 JS）提交进 Git，页面首屏需加载约 2.3MB。
- 无任何前端测试与静态检查。

---

## 二、架构分析

### 2.1 页面结构（单页应用，无路由库）

`App.jsx` 内用 `useState` 的 `activePage` 模拟路由，共 4 个模块：

```
App
├── ImageLibrary（CRM 图片处理）      ← 默认页
├── CustomerManagement（终端明细）
│    └── SnowOutboundUploadModal（内联出库上传弹窗）
├── SnowPolicyManagement（雪花出库政策）
│    └── SnowOutboundUploadModal（独立弹窗组件，与上面重复实现）
├── ProductManagement（产品明细）
│    └── ProductImportModal
└── Users（权限管理，独立文件 pages/Users.jsx）
```

组件间数据传递全部走 props，无全局状态库（合理，规模未到需要 Redux 的程度）。但**页面切换不做懒加载**，所有代码打进单个 `index-arco.js`。

### 2.2 全局依赖模式（需要特别注意）

`src/main.jsx` 只有 8 行，全部组件通过 `window.React`、`window.arco` 取用依赖：

```js
// react-globals.js
window.React = React;
window.ReactDOM = compatibleReactDOM;   // React 19 兼容层
```

**成因：** 构建脚本 `build.mjs` 用 esbuild 把 `react-globals.js` 打成 IIFE 全局包、`main.jsx` 打成 ESM 业务包，Arco 的 UMD 包（`arco.min.js`）要求 `React` 为全局变量。这带来三个隐患：

1. **React 19 兼容层是脆弱 hack**：Arco 2.66 的命令式 API（`Message`、`Modal.confirm`）内部仍调用 `ReactDOM.render`，而 React 19 已移除该 API，所以代码在 `react-globals.js` 里手写了兼容层（WeakMap 维护 root）。升级 React 或 Arco 大版本时极易踩坑，且该 hack 只保证"不崩"，不保证行为完全等价。
2. **IDE/静态分析失效**：全部依赖从 `window.*` 取，跳转、类型推断、树摇（tree-shaking）全部失效。
3. **无法做 SSR/测试**：Node 环境没有 `window`，前端测试框架需要额外 mock。

**建议：** 中期将源码改为标准 ESM 导入（`import React from "react"`、`import { Button } from "@arco-design/web-react"`），构建回归 Vite（vite.config.js 已存在但未使用），彻底移除 `react-globals.js` 兼容层。

### 2.3 构建链路

`package.json` 的 `build` 脚本执行自定义 `build.mjs`：

1. 复制 `arco.min.js` / `arco.min.css` / logo 到 `web/assets/`；
2. esbuild 打包 `react-globals.js` → `react-globals.js`（IIFE）；
3. esbuild 打包 `main.jsx` → `index-arco.js`（ESM）；
4. 生成 `web/index.html`，所有资源带 `?v=<Date.now().toString(36)>` 版本号。

问题：

- **vite.config.js 与 build.mjs 双轨**：`npm run build` 走 esbuild，`vite build` 又会往 `web/` 输出另一套产物（`emptyOutDir: false`），两套构建行为不一致，极易混淆。
- **构建产物提交进 Git**：`web/assets/` 未被 .gitignore 排除，1MB 的 arco.min.js 等每次构建都会变更提交，仓库体积膨胀且合并冲突频发。应在 CI/部署时构建，产物不进版本库。
- **`web/assets/` 里有 3 个历史遗留包**：`index-DM3rO2D0.js`（499KB）和 `index-reactmui.js`（511KB）在当前 `index.html` 中无任何引用（已核实），是旧构建残留；`arco.min.js`（1.0MB）是每次构建复制的新文件。仅清理孤儿文件即可省 1MB 仓库体积。

---

## 三、问题清单（按严重级别）

### P1 高优先级（建议 2 周内整改）

#### P1-1 年份硬编码 2026，2027 年功能将直接失效

位置：`frontend/src/App.jsx:4144`（`emptyPolicyForm` 默认 `year: 2026`）、`:4594-4595`（筛选器"年月"下拉只生成 2026 年 12 个月）、`:4930-4942`（政策表单月份下拉固定 `2026-XX`）。

影响：进入 2027 年后，用户**无法新建/筛选/编辑任何政策标签**，月份下拉也没有 2027 选项，属于确定性时间炸弹。

整改：
1. 年份由后端下发（`/api/snow-outbound/options` 返回可选年月列表），前端直接渲染；
2. 或前端基于 `new Date().getFullYear()` 动态生成 `[当前年, 当前年+1]`；
3. 同步修改筛选器与表单两处 `monthOptions` 生成逻辑，禁止字面量年份。

#### P1-2 批量提取无限轮询：无超时、无取消、卸载后仍更新状态

位置：`frontend/src/App.jsx:710-726`（`waitForJob` 的 `while (true)` + 650ms 轮询）。

问题：
- 任务若在服务端卡死（进程重启、任务队列异常），前端会**无限轮询**，每次请求都打到服务器；
- `submit()` 路径没有 `active` 卸载保护（恢复任务的效果函数有，但提交路径没有），用户关闭弹窗后轮询仍会 `setState` 已卸载组件；
- 没有"取消任务"按钮，用户无法主动终止。

整改：
1. 轮询加最大时长（如 30 分钟）或最大次数，超时提示用户；
2. `submit()` 内同样增加 mounted 标记，卸载即停止；
3. 后端提供取消接口（如 `DELETE /api/batch-extract/<job_id>`），前端加"取消"按钮；
4. 轮询间隔建议指数退避（650ms → 2s → 5s 封顶）。

#### P1-3 业务员名单前端硬编码，与后端接口双份数据源

位置：`frontend/src/App.jsx:2004-2010`（`CUSTOMER_OPTIONS` 硬编码 4+2 个业务员）、`:2198-2202`（`loadCustomerOptions` 只取 `data.routes`，**丢弃了** `data.salespeople` / `data.snow_salespeople`）、`:2614/2846/2857`（筛选与表单下拉使用硬编码名单）。

后端 `web.py:1903-1906` 的 `/api/customers/options` 其实已返回 `salespeople`、`snow_salespeople`。

影响：新增业务员时前端不显示（只能改代码），且两边名单迟早不一致；人员筛选会漏人。

整改：`loadCustomerOptions` 一并保存 `data.salespeople`/`data.snow_salespeople` 到 state，所有下拉改为 state 渲染，删除 `CUSTOMER_OPTIONS` 中的人员名单常量。

#### P1-4 `App.jsx` 5,427 行巨石文件

位置：`frontend/src/App.jsx`（整文件）。

问题：4 个业务页面、10+ 个组件、全部工具函数（日期格式化、复制、下载、CSRF）堆在一个文件，任何改动都可能误伤其他页面；代码评审 diff 无法聚焦；无组件复用边界（如 `SnowOutboundUploadModal` 与 `CustomerManagement` 内联弹窗几乎重复实现，约 200 行）。

整改（渐进式）：
1. 按现有函数边界直接拆分：`components/`（RowActions、TerminalListModal、UploadModal 们）、`pages/`（ImageLibrary、CustomerManagement、ProductManagement、SnowPolicyManagement）、`utils/`（jsonFetch、copyText、formatDateTime、downloadPostFile）；
2. 每拆一个页面验证一次构建；
3. 拆分后自然暴露重复弹窗，合并为公共组件。

#### P1-5 无前端测试、无静态检查、无 CI

位置：`frontend/package.json`（只有 dev/build/preview 三个脚本）。

后端有较完整的 pytest（`tests/test_web.py` 1500+ 行），前端是零测试。核心业务（出库导入、归档、政策命中）一旦改坏，只能靠人工回归。

整改：
1. 引入 ESLint（react-hooks 插件能立刻抓出依赖数组问题）+ Prettier，`npm run lint` 进 CI；
2. 用 Vitest + Testing Library 补关键流程测试：图片归档、雪花出库预览/提交、客户 CRUD、政策表单校验；
3. GitHub Actions 拉取仓库后 `npm ci && npm run lint && npm run test && npm run build`，构建产物不入库。

### P2 中优先级（建议 1-2 个月内整改）

#### P2-1 约 400 行死代码（被 `{false ? ...}` 永久禁用的雪花模板功能）

位置：`frontend/src/App.jsx:3021-3166`（JSX 死块），以及仅被该块调用的函数：`loadSnowTemplates`（:2222）、`selectSnowTemplate`（:2373）、`changeSnowRules`（:2390）、`updateSnowRule/updateSnowCondition/addSnowRule/addSnowCondition/removeSnowCondition`（:2395-2434）、`saveSnowTemplate/deleteSnowTemplate`（:2436-2491）、`newSnowRule/cloneRules`（:2045-2052）。

影响：代码阅读者无法区分"功能被隐藏"还是"功能已废弃"；这些函数引用的 state（snowTemplates/snowTemplateId/snowRules 等）全部冗余。

整改：若模板功能确认下线，整块删除（含相关 state 与函数、后端模板接口按需同步下线）；若计划恢复，给开关变量命名并挪到独立组件，不要用 `{false ? ...}` 硬编码。

#### P2-2 首屏资源过重（约 2.3MB），且无拆包

当前 `web/index.html` 同步加载：

| 资源 | 大小 |
|---|---:|
| `arco.min.js`（UMD 全量） | 1,024 KB |
| `react-globals.js`（IIFE） | 188 KB |
| `index-arco.js`（业务包） | 125 KB |
| `arco.min.css` | 565 KB |
| `index-arco.css` | 78 KB |
| logo PNG | 231 KB |

三个 `<script>` 均无 `defer`，阻塞首屏。整改：
1. 改用按需引入的 Arco 组件（tree-shaking），可省掉 1MB UMD 包；
2. 页面级懒加载（`React.lazy` + 动态 import），首屏只加载 CRM 图片处理页；
3. 资源加 `defer`；字体/logo 用 `loading="lazy"` 与预压缩；
4. 部署侧确认 gzip/brotli 已开启（Caddy/Nginx 配置）。

#### P2-3 表格无虚拟化，冻结列三重渲染，hover 整表重渲染

位置：`frontend/src/App.jsx:2706-2810`（客户表 + `customer-frozen-left` + `customer-frozen-right` 三份 DOM）、`:2690-2698`（`onMouseEnter/onMouseLeave` 设置 `hoveredCustomerId`，触发整表重渲染）。

影响：每页 100 条时，一次 hover 变化会重渲染 300 行 DOM；终端数上万后表格明显卡顿。产品表、政策表同理。

整改：
1. 行组件抽离并 `React.memo`，hover 状态只重渲染当前行（配合 `hoveredCustomerId` 比较）；
2. 冻结列改用 `position: sticky`（当前用 transform 手算 scrollLeft，脆弱）或引入虚拟表格（如 `@tanstack/react-virtual`）；
3. 把 `setHoveredCustomerId` 改为不触发整表渲染的事件委托方案（CSS `:hover` 或 data 属性）。

#### P2-4 CSS 重复与遗留：legacy.css 与新版样式大面积重叠

位置：`frontend/src/styles/legacy.css`（2,537 行）与 `base.css`/`common.css`/`customers.css`/`products.css`/`snow-outbound.css` 重叠：

- `html, body, #root`、`body` 背景在 `base.css` 与 `legacy.css` 重复定义；
- `.tag-neutral` 在 `common.css`（1 处）与 `legacy.css`（6 处）重复；
- `.customer-table`、`.snow-upload-modal`、`.policy-*`、`.progress-*` 等跨文件反复覆盖；
- `customers.css` 含 4 处 `!important`（:392/:399/:413/:417）。

影响：同一条规则分散在多个文件，改样式要"全文搜索"才能确认生效位置，且 `!important` 会掩盖层叠问题。

整改：以 tokens.css 为准做一次样式清理，逐类删除 legacy.css 中被新版覆盖的规则，保留真正遗留的页面规则并改名为 `legacy-` 前缀，最终目标是删除 legacy.css。

#### P2-5 前端校验与后端校验双份实现，且部分不一致

例如：终端编码 10 位数字（前端 `:2565` + 后端都校验，OK）、产品规格 `\d+`、库存非负、结算价 2 位小数前端正则宽松（`/^\d*(\.\d{0,2})?$/` 允许空串由 `formReady` 放行，后端 `Number()` 转换行为可能不同）。

整改：核心校验收敛到后端，前端只做轻量反馈；或抽共享校验 schema（如 zod）前后端复用。

#### P2-6 `personColorClass` 为桩实现

位置：`frontend/src/App.jsx:2075-2077`，恒返回 `"person-gray"`；CSS 只有 `.person-tag.person-gray`（customers.css:499）。业务员彩色标签功能实际未完成。

整改：要么按人员名生成稳定色类（如 `person-blue/red/green` 各定义样式），要么删除函数与多余 CSS，避免"看起来支持彩色、实际全灰"。

#### P2-7 通知按钮恒显示红点却提示"暂无通知"

位置：`frontend/src/App.jsx:5352-5359`（`notification-dot` 无条件渲染）。

影响：用户会习惯性忽略红点，将来接入真实通知时无人点击。整改：无通知时不渲染红点，或直接移除该按钮。

#### P2-8 无错误边界与前端错误监控

任一页面组件运行时抛错（如接口返回结构变更），整个工作台白屏，且无任何上报。Arco 命令式 API 崩溃只能靠 react-globals 的兼容层兜底。

整改：包一层 `ErrorBoundary`（显示降级 UI + 刷新按钮），接入轻量错误上报（自建 `/api/frontend-error` 或 Sentry）。

#### P2-9 无障碍遗留问题

已做得不错（按钮基本有 `type="button"`、主要交互有 `aria-label`、支持 `prefers-reduced-motion`、focus-visible 样式）。遗留：

- 全屏图片预览（`App.jsx:1997-2012`）无焦点陷阱（focus trap），Tab 可跳出 dialog；
- 政策"照片核验"用 `span role="checkbox" aria-readonly`（:4747），语义可用但建议直接展示 Icon/标签；
- `TableEllipsis` 对 `-` 值设置 `aria-label` 为 undefined，但 Tooltip 未展示，键盘用户拿不到信息；
- 部分按钮文案为符号（`⋯`、`⌄`）虽带 `aria-label`/`title`，建议统一。

### P3 低优先级

- **无 TypeScript/PropTypes**：`RowActions` 的 `items`、各 Modal 的 props 无类型约束，重构易出错。
- **无路由库**：`activePage` 状态切换无法收藏/分享具体页面 URL，刷新后回到默认页；建议引入轻量 hash 路由。
- **`Intl.DateTimeFormat` 每次渲染新建**（`App.jsx:205`）：表格 100 行 × 多次渲染开销可忽略，但可缓存为模块级实例。
- **DEV/生产标题不一致**：`frontend/index.html` 标题为"雪花CRM拜访照片提取"，生产为"鑫向晨商贸工作台"。
- **无 favicon**：生产 `index.html` 无 `<link rel="icon">`。
- **上传仅前端扩展名校验**：`accept=".xlsx"` 只是建议，类型校验依赖后端（后端已做），可补充文件大小上限提示。
- **`/api/session` 每次写操作前额外请求**：`latestCsrfToken()` 每次保存/删除都先 GET 一次会话，属小冗余；可直接用页面初始 token 并处理 403 刷新。
- **提交信息与历史**：`bae70aa` 一次提交新增 5,427 行 `App.jsx` 并删除 5,547 行 `main.jsx`，粒度过大，回滚/审查困难；建议后续按功能拆 commit。

---

## 四、安全专项分析

### 4.1 做得好的部分

- **XSS**：React 默认转义，全库无 `dangerouslySetInnerHTML` / `innerHTML` 拼接；登录页模板只替换固定错误文案与随机 CSRF token。
- **CSRF**：所有写接口同时校验 `X-CSRF-Token` 头 + body/form 字段，后端 `hmac.compare_digest` 比对；会话 Cookie `HttpOnly + SameSite=Lax`，生产环境 `Secure`（`web.py:1605-1607`）。
- **安全头**：`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: no-referrer`、`Permissions-Policy`、CSP `frame-ancestors 'none'`（`web.py:1629-1641`）。
- **鉴权闭环**：所有页面与 API 走 `_login_required`；管理功能（用户管理、删除）后端有 `_admin_required` 兜底，前端禁用只是体验层。
- **下载**：导出走 POST + CSRF，文件名从 `Content-Disposition` 解析，`URL.revokeObjectURL` 有回收。
- **敏感信息**：客户手机号在详情弹窗中脱敏（`maskPhone`）；仓库未提交 `.env`。

### 4.2 建议改进

1. **收紧 CSP**：当前 `script-src 'self' 'unsafe-inline'`（`web.py:1637`）。检查后生产页面（`web/index.html`、`web/login.html`）均无内联 `<script>`，可将 `unsafe-inline` 从 script-src 移除，只保留 `style-src 'unsafe-inline'`（Arco 需要内联样式）。
2. **移除 assets 接口的 CORS 通配**：`web.py:1827` 对 `/assets/<path>` 设置 `Access-Control-Allow-Origin: *`。同源部署不需要 CORS，且 `*` 允许任意站点读取（无凭据的）静态资源，建议删除。
3. **依赖安全审计**：`package-lock.json` 已锁定版本，但仓库无 `npm audit` 记录。建议 CI 加 `npm audit --audit-level=high`（需网络），并定期升级。
4. **登录暴力破解**：前端无速率限制提示（后端 `_check_rate_limit` 仅用于提取接口）；若密码模式暴露公网，建议登录接口也加限流与失败锁定。
5. **密码策略**：目前仅"至少 6 位"（users.py:133），建议至少 8 位并含字母数字，前端提示同步。

---

## 五、性能专项分析

### 5.1 已做对的

- 图片库每页 12 个终端，`loading="lazy"` + `lazyload`；
- `requestIdleCallback` 预取相邻页，并带缓存上限（`App.jsx:961-973`）；
- 列表请求带竞态序号（`requestSequence`/`loadRequestRef`），避免旧响应覆盖新响应；
- 分页后回到顶部用双 rAF 平滑处理；
- 批量任务进度轮询、localStorage 断点恢复。

### 5.2 待优化

1. **首屏 2.3MB**（见 P2-2）：建议 Arco 按需引入 + 页面懒加载 + gzip。
2. **表格渲染**（见 P2-3）：三重 DOM + hover 全表重渲染。
3. **大列表无虚拟化**：客户表 100 条/页尚可，但冻结列方案是 transform 手算，滚动性能差，建议 sticky 化。
4. **无图片尺寸规范**：`Image` 直接用原图/缩略图 URL，未指定宽度高度占位，懒加载时布局跳动（虽有 CSS 网格兜底）。
5. **`index-arco.js` 单包无 split**：所有页面代码一次下载，后续页面增多时应按路由分包。

---

## 六、可维护性与工程化

### 6.1 现状

- 无 lint / format / typecheck / test / CI；
- 代码全部通过 `window.*` 全局依赖（见 2.2）；
- 业务常量散落前端（业务员、政策标签、售卖类型、2026 年），与后端重复；
- CSS 四代样式共存（tokens → 各页面 → legacy）；
- 构建产物入库，双构建脚本并存；
- 仓库双分支形态不一致（main 为旧单文件版）。

### 6.2 建议的工程化基线

1. `npm run lint`（eslint-plugin-react-hooks）+ `npm run format`（prettier）+ `npm run test`（vitest）；
2. Git pre-commit hook（husky + lint-staged）；
3. GitHub Actions：`npm ci → lint → test → build`，产物留作 artifact 或推送部署机；
4. `.gitignore` 增加 `web/assets/` 与 `web/index.html`（构建生成物），统一走构建流程；
5. 将 `main` 分支归档，声明 `codex/add-wecom-distribution-workflow` 为唯一发布分支。

---

## 七、整改路线图

### 短期（2 周内，止血）

- [ ] 删除 `web/assets/index-DM3rO2D0.js`、`index-reactmui.js` 两个孤儿包（省 1MB）；
- [ ] 修复年份硬编码 2026（P1-1）；
- [ ] 批量轮询加超时/取消/卸载保护（P1-2）；
- [ ] `loadCustomerOptions` 接入后端下发的业务员名单（P1-3）；
- [ ] 收紧 CSP script-src、删除 assets 的 `Access-Control-Allow-Origin: *`（安全项）；
- [ ] 删除 `{false ? ...}` 死代码块（P2-1）；
- [ ] 运行 `npm audit`，升级高危依赖。

### 中期（1-2 个月，结构优化）

- [ ] 拆分 `App.jsx` 为 pages/components/utils（P1-4）；
- [ ] 引入 ESLint + Prettier + Vitest，补核心流程测试（P1-5）；
- [ ] 引入 TypeScript（可从 `strict` 新文件开始渐进迁移）；
- [ ] Arco 按需引入 + 页面懒加载，首屏减重（P2-2）；
- [ ] 行组件 `React.memo`、表格 sticky 冻结列（P2-3）；
- [ ] 清理 legacy.css 与重复规则（P2-4）；
- [ ] 修复 personColorClass 桩实现与通知红点（P2-6/P2-7）；
- [ ] 增加 ErrorBoundary 与前端错误上报（P2-8）。

### 长期（持续演进）

- [ ] 统一构建链路（Vite build，产物不入库，CI 构建）；
- [ ] 页面级虚拟化（数据量大后）；
- [ ] 引入路由（hash 起步）与代码分包；
- [ ] 前端表单校验 schema 化，与后端共享；
- [ ] 依赖升级策略：Renovate/Dependabot + 定期大版本升级验证（重点验证 React/Arco 兼容层是否可移除）。

---

## 八、亮点清单（建议保留）

1. CSRF 双重校验 + 安全响应头 + Cookie 属性配置，是内部工具里少见的标准姿势。
2. 图片库的分页预取、缓存上限、竞态防护，工程意识在线。
3. "解析预览 → 确认导入"两步式上传交互（雪花出库、产品导入），用户可先看结果再提交。
4. 批量任务断点恢复（localStorage 记录 job id），页面刷新不丢任务。
5. 终端编码"复制全部"做成 Excel 单列格式 + 全选 textarea，贴合业务操作习惯。
6. 手机号脱敏、下载文件名解析、blob URL 回收等细节到位。
7. 前端对可访问性有明确投入（aria-label、focus-visible、reduced-motion），底子比多数内部系统好。

---

## 附录 A：关键文件与行号索引

| 问题 | 位置 |
|---|---|
| 年份硬编码 | `frontend/src/App.jsx:4144`、`:4594`、`:4930` |
| 无限轮询 | `frontend/src/App.jsx:710-726` |
| 业务员硬编码 | `frontend/src/App.jsx:2004-2010`、`:2198-2202`；后端 `web.py:1903-1906` |
| 死代码块 | `frontend/src/App.jsx:3021-3166` |
| 巨石文件 | `frontend/src/App.jsx`（5,427 行） |
| 重复弹窗 | `frontend/src/App.jsx:2830-3204`（内联）与 `:3206-3406`（独立组件） |
| React 兼容层 | `frontend/src/react-globals.js` |
| 构建脚本 | `frontend/build.mjs`；`frontend/vite.config.js` |
| 依赖与脚本 | `frontend/package.json` |
| CSS 重复 | `frontend/src/styles/legacy.css`（2,537 行）与各页面 CSS |
| CSP/安全头 | `web.py:1629-1641` |
| Session Cookie | `web.py:1605-1607` |
| Assets CORS | `web.py:1825-1827` |
| 后端业务员数据源 | `web.py:1903-1906` |

